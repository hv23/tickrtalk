var invitees = $('#invitees').val();

if (invitees == "") {
	$("#invitees").val($("#invitees").val() + "<%= @followed %>");
	$("#follows").val("");
} else {
	$("#invitees").val($("#invitees").val() + ", <%= @followed %>");
	$("#follows").val("");
}